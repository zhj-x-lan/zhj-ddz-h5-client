var soundMngr = require('SoundMngr');
cc.Class({
    extends: cc.Component,

    properties: {
         pokerId:0,
         index:0,
    },

    // use this for initialization
    onLoad: function () {
        
      
    }
});